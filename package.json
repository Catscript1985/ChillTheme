{
  "name": "chilltheme",
  "version": "0.1.0",
  "description": "Personal wallpaper library for Windows, macOS and Linux",
  "author": "ChillTheme Contributors <noreply@github.com>",
  "main": "electron/main.cjs",
  "type": "commonjs",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "build": "electron-builder",
    "check": "node --check electron/main.cjs && node --check src/app.js"
  },
  "keywords": ["wallpaper", "windows", "gif", "electron"],
  "license": "MIT",
  "build": {
    "appId": "com.chilltheme.desktop",
    "productName": "ChillTheme",
    "artifactName": "ChillTheme-${os}-${arch}.${ext}",
    "asar": true,
    "files": ["electron/**/*", "src/**/*", "package.json"],
    "win": {
      "target": ["nsis", "portable"],
      "artifactName": "ChillTheme-Windows-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    },
    "mac": {
      "target": ["dmg", "zip"],
      "category": "public.app-category.utilities",
      "artifactName": "ChillTheme-macOS-${arch}.${ext}"
    },
    "linux": {
      "target": ["AppImage", "deb"],
      "category": "Utility",
      "artifactName": "ChillTheme-Linux-${arch}.${ext}"
    }
  },
  "devDependencies": {
    "electron": "^38.1.0",
    "electron-builder": "^26.0.12"
  }
}
